---
title: Hi, I’m Cédric
permalink: /index.html
type: homepage
layout: base.njk
---
{% import 'list-items.njk' as list %}

<div class="o-container o-box o-prose u-lede">
Hi, I’m Cédric a swiss front end engineer based in Lausanne with a love for electronic music and hiking
</div>

<section class="o-container o-box o-stack">
    <header class="o-prose">
        <h2>Articles</h2>
    </header>
    <div>
        {%- set latest_articles = collections.article | reverse -%}
        {{ list.listItems(latest_articles, "", 5) }}
    </div>
    <footer class="o-prose">
        <p><a href="{{- "/articles" | url -}}">More articles</a></p>
    </footer>
</section>

<section class="o-container o-box o-stack">
    <header class="o-prose">
        <h2>Notes</h2>
    </header>
    <div>
        {%- set latest_notes = collections.note | reverse -%}
        {{ list.listItems(latest_notes, "o-stack--space-3xl", 5) }}
    </div>
    <footer class="o-prose">
        <p><a href="{{- "/notes" | url -}}"">More notes</a></p>
    </footer>
</section>

<section class="o-stack">
    <header class="o-container o-box o-prose">
        <h2>Photos</h2>
    </header>

    {% set latest_photos = collections.photo | reverse %}
    {%- import 'image.njk' as image -%}

    <div class="o-container o-container--full">
        <div class="o-reel">
        {%- for item in latest_photos.slice(0,10) -%}
            <a href="{{item.url}}">
                {{- image.image(item.data.photo[0].url, item.data.photo[0].alt, settings=srcsets.grid, appUrl=app.url, styleModifier="c-scale-hover__item") -}}
            </a>
        {%- endfor -%}
        </div>
    </div>
    <footer class="o-container o-box o-prose">
        <p><a href="{{- "/photos" | url -}}"">More photos</a></p>
    </footer>
</section>
