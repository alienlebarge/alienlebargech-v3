<header class="o-prose o-stack o-stack--s0 c-header{% if vocab and type != "note" %} | p-name{% endif %}">
    <h1>{{ title }}</h1>
    {% if summary -%}
        <p class="u-lede {% if vocab %} | p-summary{% endif %}">
        {{ summary }}
        </p>
    {%- endif %}
</header>
