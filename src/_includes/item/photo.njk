<article>
    <a href="{{ item.url | url }}">

        {# Set vars for the image #}
        {%- set src = item.data.photo[0].url -%}
        {%- set alt = item.data.photo[0].alt -%}
        {% include 'image.njk' %}
    </a>
</article>
